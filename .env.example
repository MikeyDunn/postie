# Local copies for CLI probes/scripts only — the deployed Lambdas read Slack
# credentials from SSM (/postie/slack/*).
SLACK_BOT_TOKEN=xoxb-...
SLACK_SIGNING_SECRET=...

# App code does NOT read this key — each workspace adds its own via /postie setup.
MAILSTREAM_API_KEY=...
